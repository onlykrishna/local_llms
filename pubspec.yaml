name: offline_ai_flutter_demo
description: A Flutter demo app showcasing offline AI with local LLMs using Clean Architecture and GetX.
publish_to: 'none' # Prevent accidental publishing
version: 1.0.0+1
environment:
  sdk: '>=3.2.0 <4.0.0'

dependencies:
  flutter:
    sdk: flutter
  get: ^4.6.6
  get_storage: ^2.1.1
  hive: ^2.2.3
  hive_flutter: ^1.1.0
  http: ^1.2.0
  google_fonts: ^6.2.1
  markdown_widget: ^2.0.0
  flutter_markdown: ^0.6.14+1
  flutter_spinkit: ^5.2.0
  path_provider: ^2.1.2
  uuid: ^4.2.2
  intl: ^0.19.0

  # For offline fallback dataset
  json_annotation: ^4.9.0
  # Add any other needed packages

dev_dependencies:
  flutter_test:
    sdk: flutter
  hive_generator: ^2.0.1
  build_runner: ^2.4.6
  json_serializable: ^6.8.0

flutter:
  uses-material-design: true
  assets:
    - assets/fallback_dataset.json
